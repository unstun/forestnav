---
citation_key: Huo2021IntegerProgrammingBased
arxiv_id: 2107.12219
arxiv_url: https://arxiv.org/abs/2107.12219
source: arxiv-e-print
converter: pandoc-3.9 markdown+tex_math_dollars-raw_html-raw_tex
converted_at: 2026-06-24T16:37:20Z
origin: ai+web
reviewed: false
---

::: IEEEkeywords
Path Planning for Multiple Mobile Robots or Agents, Multi-Robot Systems, Collision Avoidance, Logistics.
:::

# Introduction

We study the Multi-robot Path Planning (MPP) problem which has been explored for decades, focusing on developing a high efficient algorithm with heuristics. In this work, the robots are uniquely labeled (i.e., distinguishable). In our distinguishing narrow-passage warehouse-like environment setting, the majority of vertices in the map are occupied by shelves. Passages between shelves are narrow, i.e., the width of passages is one free vertex. Here, by narrow-passages, we means that each two robots cannot move side by side, as shown in Figure. [1](#warehouse){reference-type="ref" reference="warehouse"}. Robots should move from its current vertex to an adjacent free one in one time step without robot collision. Robot collision occurs when two robots simultaneously move to the same free vertex or along the same edge in opposing directions. Here, we study two common optimality objectives focusing on distance optimality. Distance optimality objectives have been proven to be NP-hard [@yu2015intractability].

In this work, we propose the complete **OMRPP** algorithm and effective heuristics in order to generate collision-free robot path set efficiently in the narrow-passage warehouse-like environment. Applying the same optimality ratio definition in [@han2020ddm], collision-free path generated by **OMRPP** has 1.x optimality ratio with efficient running time performance.

**Contributions.** This study brings two main contributions:

1.  Based on narrow-passage warehouse-like environment, we propose an IP-based multi-robot path planning framework. The pipeline of this framework can be applied effectively to high-density environment and produces anytime-feasible solutions.

2.  We introduce a heuristic method to boost the effectiveness of the proposed IP-based framework. In polynomial running time complexity, the solutions of the heuristic method can be achieved within performance boundary.

# Related works

Nowadays Multi-Robot Systems (MRS) have found many applications in the real environment, e.g., logistics warehouses [@wurman2008coordinating; @han2019effective], in order to improve the scheduling efficiency. MRS application studies can also be found in pickup and delivery [@liu2019task], localization [@fox2000probabilistic], communication and coordination [@luna2010network]. Path planning based on MRS (briefly MPP) is an optimization problem which has been explored for decades [@erdem2013general; @erdmann1987multiple; @lavalle1998optimal; @fox2000probabilistic; @schouwenaars2001mixed]. MPP studies can be found its initial mathematical representation on the classical 15-puzzle moving problem [@johnson1879notes; @loyd1959mathematical; @archer1999modern], studies have found its complexity in [@goldreich2011finding]. In this work, we focus MPP problems on the stable environment where the shelves are fixed. Readers can refer to [@wurman2008coordinating; @van2008reciprocal] for dynamic environment setting, e.g., moving obstacles and dynamic task assignments.

Multiple algorithms have been proposed to solve MPP problems. MPP solvers like Conflict-Based Search (CBS) [@sharon2015conflict] and Exact Mixed Integer Programming (MIP) [@schouwenaars2001mixed] can be used to generate paths set for MRS optimally. Optimal solvers (e.g., CBS and MIP) generate optimal solutions at the cost of running time. In order to improve solving efficiency, polynomial-time algorithms are proposed like Priority Inheritance with Backtracking (PIBT) [@okumura2019priority], push-and-swap-and-rotate [@de2013push]. Compared with optimal algorithms, these polynomial algorithms solve MPP problems fast while optimality is lost. In order to balance optimality and running time, sub-optimal solvers like Integer Linear Programming with splits [@yu2016optimal; @han2019integer] and Enhanced CBS (ECBS) [@barer2014suboptimal] are designed for efficient solving process.

In our environment setting, narrow passages impose challenges to the MPP algorithms. Partial splitting heuristic methods in [@han2019integer] may lose efficiency as possible alternative vertices may be excluded by splitting heuristic methods when treating robot collisions in narrow passages. Here, as we note in the main context, robot collisions in narrow passages are avoided by assigning directions to passages, similar ideas can be seen in [@wang2008fast; @cohen2016improved]. To the best of our knowledge, we are the first to apply optimized passage directions in MPP problems rather than fixed [@wang2008fast] or heuristic weighted passage directions [@cohen2016improved].

# Preliminaries

## Multi-robot path planning in narrow-passage environment

Let $G=(V,E)$ be an undirected grid map, with $V := \{(i,j)| M(i,j) = 0\}$ being the vertex set. For each $(i,j) \in V$, its neighborhood is $N\big((i,j)\big):=\{(i+1,j),(i,j+1),(i-1,j),(i,j-1)\}\cap V$ using *4-way neighbourhood*. Here, $E:=\{(v_i,v_j)| v_i \in V \wedge v_j\in N(v_i)\}$ is defined as the edge set. In this work, we suppose that a bijection exists between $V$ and all the grid cells represented by $M$. Let $R=\{r_1,\dots,r_n\}$ be a set of $n$ robots. A *configuration* of the robots is an injective map from $R$ to $V$, i.e., any two robots $r_i$ and $r_j$ $(i\neq j)$ occupy different vertices of $V$. At any given time step $t\in \{0,1,\dots,\}$, the robots assume a configuration. The initial configurations and goal configurations of the robots are denoted as $X_I$ and $X_G$.

For a robot $r \in R$ with initial and goal configurations $X^I(r),\, X^G(r) \in V$, a scheduled path is defined as a sequence of vertices $P_r := \left\{p_r\left(t_r^0\right),
p_r\left(t_r^0+1\right),\dots,
p_r\left(t_r^0 + {T_r}\right)\right\}$, $p_r(t) \in V$ satisfying:

1.  $p_r \left(t_r^0\right)=X^I(r)$;

2.  $p_r\left(t_r^0+{T_r}\right)=X^G(r)$;

3.  $\forall t, t_r^0+1\leq t \leq t_r^0+T_r$, $p_r(t)\in \{p_r(t-1)\}\cup N\left(p_r(t-1)\right)$.

Between time step $t-1$ and $t$, robot $r$ occupies an edge $\big(p_r(t-1),p_r(t)\big) \in E$. The scheduled path set of $R$ is $P:=\{P_1,\dots,P_n\}$. We say that two paths $P_i,P_j$ are in collision if there exists $t\in \mathbb{Z}^+$ such that $p_i(t)=p_j(t)$ (meet collision) or $\left(p_i(t-1),p_i(t)\right) = \left(p_j(t),p_j(t-1)\right)$ (head-on collision). A path $P_i$ is *feasible* if no other $P_j$ is in collison with $P_i$.

Fig. [1](#warehouse){reference-type="ref" reference="warehouse"} shows an example with the green blocs A/D show a portion of a collision-free path, while the red blocs C/E show head-to-head collisions and the yellow bloc B shows a crossing collision.

::: {#OriginPro .problem}
****Problem** 1**. ***(Original MPP problem)**: Given a 3-tuple $\langle G,X^I,X^G\rangle$, find a path set $P=\{P_1,\dots,P_n\}$ such that for all $1\leq i\leq n$ satisfying: $P_i$ is feasible.*
:::

In a warehouse, a robot starts from a occupied cell carrying the shelf. Once leaving the occupied cell, it enters a cell in a narrow passage, which is its initial configuration $X^I(r)$. When robot $r$ arrives at $X^G(r)$, it will go under a nearby shelf and disappear from the passages. In such scenarios, we present the following assumption:

::: {#assum1 .assum}
****Assumption** 1**. *A robot $r$ occupies a vertex of $G$ only during $t_r^0$ to $t_r^0+ T_r$, i.e., robot $r$ is not an obstacle for other robots for all $t, t \notin \{t_r^0,t_r^0+1,\dots,t_r^0+T_r\}$.*
:::

In most of the existing MPP algorithms, the time step, i.e., $t_r^0, t_r^0+1, \dots, t_r^0+T_r$ are also an important part of the planning results of Problem [1](#OriginPro){reference-type="ref" reference="OriginPro"}. That is, robot $r$ should arrive at $p_r(t)$ at the time step $t$ exactly. However, in real scenarios, it is very difficult to require the robots follow the scheduled paths in step consistently. For example, the original planning result requires that each robot should move one grid cell at each time step. However, in the real scenarios, the robots may move at different velocities due to, e.g., the different weights of their loads. In such case, even only one of the robots cannot follow its scheduled path in step consistently, collisions may happen in a narrow passage. To avoid collisions in such case, all the robots should take the scheduled motions strictly. This issue motivates us to consider a variant of Problem [1](#OriginPro){reference-type="ref" reference="OriginPro"} as follows. Let us introduce the following concept first.

::: definition
****Definition** 1**. *An *anytime-feasible path set* $P$ is define as: $$\begin{align*}
    P_r =\{p_r(t_r^0), p_r(t_r^0+1), \dots, p_r(t_r^0+T_r)\},\\p_r(t_r^0)=X^I(r),p_r(t_r^0+T_r)=X^G(r)
\end{align*}$$ for all $r \in \{1,2\dots,R\}$ such that for any $i,j \in \{1,2\dots,R\}$, paths $P_i$ and $P_j$ are feasible for any $t_i^0 \in \mathbb{N},T_i \in \mathbb{N}, t_j^0 \in \mathbb{N}$ and $T_j \in \mathbb{N}$.*
:::

For an anytime-feasible path set, each robot only needs to follow its resulting path at any time steps.

::: {#pb .problem}
****Problem** 2**. ***(Anytime feasible MPP problem)**: Given a 3-tuple $\langle G,X^I,X^G\rangle$, find a path set $P=\{P_1,\dots,P_n\}$ such that for all $1\leq i\leq n$ satisfying: $P_i$ is *anytime-feasible*.*
:::

## Optimal Formulation

Let $P=\{P_1,\dots,P_n\}$ be an feasible solution to some fixed MPP instance. For a path $P_i\in P$, let $len(P_i)$ denotes the length of the path $P_i$, which is the total number of times the robot $r_i$ changes its residing vertex while following $P_i$. A robot which follows a path $P_i$ may visit the same vertex multiple times. In this work, we examine two common objectives focusing on distance optimality and try to improve the time optimality under the fact that distance optimality and time optimality can not always be globally optimized simultaneously [@yu2015intractability].

*Objective 1: **TDMPP*** **(Total Distance)**: Compute a anytime-feasible path set $P$ that minimizes $\sum_{i=1}^nlen(P_i)$.

*Objective 2: **MDMPP*** **(Maximum Distance)**: Compute a anytime-feasible path set $P$ that minimizes $\max_{1\leq i \leq n}len(P_i)$.

Here, we note that if the same vertex appears twice consecutively in $P_i$, $len(P_i)$ only accumulates once.

## Amazon warehouse-like environment representation {#env}

We consider an amazon warehouse-like grid environment which is represented by a binary matrix $M$ of dimension $h\times w$. We place $S$ shelves in the environment. And we use $M(i,j)=1$ to represent that the grid cell $(i,j)$ is occupied. To maximum the storage capacity of the warehouse area, most of the cells are occupied. In order to allow the robots to move with the warehouse, some cells are left empty as passage. In Fig. [1](#warehouse){reference-type="ref" reference="warehouse"}, we define the horizontal and vertical passage index sets $\mathcal{H}$ and $\mathcal{V}$ as $\mathcal{H} := \{i_1, i_2, \dots\} \subset \{1,2,\dots,h\}$ and $\mathcal{V} := \{j_1, j_2, \dots\} \subset \{1,2,\dots,w\}$. Here both $\mathcal{H}$ and $\mathcal{V}$ are ordered sets and neither $\mathcal{H}$ nor $\mathcal{V}$ is empty. We let $M(i,j)=0$ for all $(i,j)\in \mathcal{H}\times \mathcal{V}$. For simplicity, the size of each shelf block is fixed as $h_s\times w_s$ in the following discussion. We choose $h_s=2$ and $i_1=1, j_1=1$ to allow the robots to approach every shelf. And we choose $w_s\in\{1,2,\dots,w-2\}$ such that all $j_m,j_{m+1}\in \mathcal{V}, j_{m+1} - j_m=w_s+1 =: W_s$ and for all $i_n,i_{n+1}\in \mathcal{H}, i_{n+1}-i_n = h_s+1 = 3 =: H_s$. In this case, the width of passage in this environment is 1 so we call it a narrow-passage environment.

![Amazon warehouse-like environment in a bird's eye. The horizontal and vertical passage intex sets are $\mathcal{H}=\{1,4,7\}\subset \{1,\dots,7\}$ and $\mathcal{V}=\{1,7,13\}\subset \{1,\dots,13\}$ with $H_s=3$ and $W_s=6$.](Huo2021IntegerProgrammingBased_figs/warehouse3.png){#warehouse width="100%"}

## One-way constraint

In this work, we focus on the narrow-passage amazon warehouse-like environment above. Based on the assumptions above, in order to tackle the MPP problem in narrow-passage environment, we propose the particular lemma as follows.

::: {#lemma1 .lemma}
****Lemma** 1**. ***One-way constraint** Under Assumption [1](#assum1){reference-type="ref" reference="assum1"}, to obtain an anytime-feasible path set $P$ on $G$, it requires that all possible robots pass each edge $e\in E$ in a unique direction. That is, for all $r\in R, t^0_r\in \mathbb{N},T_r\in \mathbb{N}$, if edge $\left(p_r(t),p_r(t+1)\right)$ exists in any $P_r$, the reversed edge $\left(p_r(t+1),p_r(t)\right)$ cannot exist in any other individual path of $P$.*
:::

::: Proof
**Proof 1**. *By contradiction, if $\big(p_r(t+1),p_r(t)\big)$ also exists in another path $P_s$, then by changing $t_s^0$, then it is always possible to generate collision between $P_r$ and $P_s$. $\blacksquare$*
:::

If robots collide in the crossings of passages, simple waiting strategy can be used to resolve the collision, i.e., robot near one crossing should wait until no robot in this crossing. Here, time optimality TTMPP (Total Time MPP) and MTMPP (Maximum Time MPP) can be achieved directly.

# General Methodology

In this work, our methodology for multi-robot path planning problems can be divided generally into a three-step process, which is outlined in Algorithm [\[pipeline\]](#pipeline){reference-type="ref" reference="pipeline"}. The proposed MPP problems are NP-hard, we introduce specific methods to simplify the problems. The key idea of methodology is constructing IP model based on the one-way constraint, thus, we call the methodology as **OWIP** (One-way Integer Programming).

![Complete algorithm process to get path set $P$ (other robots are omitted for clearness).](Huo2021IntegerProgrammingBased_figs/projection.png){#project width="\\linewidth"}

::: algorithm
**Topological extraction**: $\bar{X}^I, \bar{X}^G,\bar{G}\leftarrow \mathrm{Extract}\left(X^I,X^G,G\right)$

**IP model formulation**: $\bar{P}\leftarrow \mathrm{IPsolver}\left(\bar{X}^I,\bar{X}^G,\bar{G}\right)$

**Projection**: $P \leftarrow \mathrm{Project}(\bar{P},X^I,X^G,G)$
:::

## Topological Extraction

As a direct result of Lemma [1](#lemma1){reference-type="ref" reference="lemma1"}, all the robots should pass each narrow passage in one fixed direction, which we call the *one-way constraint*, and collisions in narrow passages can be avoided. Under such case, robots' motions in one narrow passage can be determined by two vertices $v_i, v_j$ at both ends and one vertex $v_p$ in the passage. Inspired by [@nguyen2017generalized], each passage can be compressed into an meta vertex along with two vertices in both ends of passages, which can significantly simplify the path planning.

Here, as we denote in Fig. [2](#project){reference-type="ref" reference="project"}, we call $v_i, v_j$ as crossing vertices and $v_p$ as passage vertex, the passage through $v_p$ can be denoted as $PA(v_p)$, whose length is $|PA(v_p)|$. A single passage vertex $v_p\in V_p$ is set for each individual passage, $V_p$ is the set of passage vertex, a crossing vertex $v_c\in V_c$ is the set for each crossing of passages in $G$, $V_c$ is the set of crossing vertex. In the first subfigure of Fig. [2](#project){reference-type="ref" reference="project"}, green squares represent the passage vertices, pink squares represent the crossing vertices of passages in $G$. In the second subfigure, green circles represent the passage vertices, pink circles represent the crossing vertices in $\bar{G}$, black arrow indicates the one-way direction of passage. In the last subfigure, final path is achieved.

Therefore, with the similar definition of $G=(V,E)$, we introduce the *topological map*:

::: definition
****Definition** 2**. *The *topological map* associated with $G=(V,E)$ is defined as: $\bar{G}=(\bar{V},\bar{E})$ whose vertex set is $\bar{V}:= V_c\cup V_p$.*
:::

For all vertices in $X^I$ and $X^G$, we find their corresponding nearest vertices in $\bar{V}$:

For each $r\in R$, if $X^I(r)\in V_c$ $\left(X^G(r)\in V_c\right)$, $\bar{X}^I(r)=X^I(r)$ $\left(\bar{X}^G(r)=X^G(r)\right)$; else $\bar{X}^I(r)=\arg\min_{v\in V_p}|v-X^I(r)|$ $\left(\bar{X}^G(r)=\arg\min_{v\in V_p}|v-X^G(r)|\right)$.

Here, $|v_i-v_j|$ denotes the Manhattan distance between two vertices. Note that all the proposed $G=(V,E)$ can be extracted into $\bar{G}=(\bar{V},\bar{E})$ and $X^I,X^G$ can be transformed to $\bar{X}^I,\bar{X}^G$ in low polynomial time, which is shown in Fig. [2](#project){reference-type="ref" reference="project"}. As will be discussed, with $\bar{X}^I,\bar{X}^G$, the IP model will be formulated and solved in $\bar{G}$ without loss of generality. Thus, the number of decision variables in proposed IP model can be eliminated significantly.

## Topological-map-based IP model formulation

There are studies [@schouwenaars2001mixed; @han2019integer; @yu2016optimal] showing that the path planning problem can be rewritten as a linear program with integer constraints that count for the path continuity and collision avoidance. A key benefit of this IP approach is that the path planning optimization can be readily solved using the GUROBI optimization software with an MATLAB interface.

**(Path continuity constraints)** On topological map $\bar{G}$, a path set $\bar{P}$ containing the scheduled non-cyclic paths for all the robots: $\forall t^0_r\leq i< j\leq t^0_r+T_r, p_r(i)\neq p_r(j)$. Given $\langle \bar{G},\bar{X}^I,\bar{X}^G\rangle$, the topological-map-based IP model introduces a 3-D binary variable $x_{v_i,v_j,r}$ for each edge $(v_i,v_j)\in \bar{E}$ to indicate whether $\bar{P}_r$ uses $(v_i,v_j)$, i.e., whether robot $r$ passes $(v_i,v_j)$. For all $r\in R$, the following constraints must be satisfied: $$\begin{equation}
    \label{SG1}
        \sum_{v_i\in \bar{V},v_i\neq \bar{X}^I(r)}x_{\bar{X}^I(r),v_i,r}=\sum_{v_i\in\bar{V},v_i\neq \bar{X}^G(r)}x_{v_i,\bar{X}^G(r),r}=1;
\end{equation}$$

$$\begin{equation}
     \label{SG2}
        \sum_{v_i\in \bar{V},v_i\neq \bar{X}^I(r)}x_{v_i,\bar{X}^I(r),r}=\sum_{v_i\in\bar{V},v_i\neq \bar{X}^G(r)}x_{\bar{X}^G(r),v_i,r}=0;
\end{equation}$$

$$\begin{equation}
     \label{continue}
        \forall v_i\in \bar{V}\backslash \{\bar{X}^I(r),\bar{X}^G(r)\},
        \sum_{v_j\in \bar{V}}x_{v_i,v_j,r}=\sum_{v_j\in\bar{V}}x_{v_j,v_i,r}\leq 1;
\end{equation}$$

Here, constraint ([\[SG1\]](#SG1){reference-type="ref" reference="SG1"}) and ([\[SG2\]](#SG2){reference-type="ref" reference="SG2"}) make $\bar{P}_r$ starts from $\bar{X}^I(r)$ and terminates at $\bar{X}^G(r)$. Constraint ([\[continue\]](#continue){reference-type="ref" reference="continue"}) ensures that for each vertex, one outgoing edge can be used if and only if an incoming edge is used. The constraints above ensure the continuity of $\bar{P}_r$ and forces each vertex to appear in $\bar{P}_r$ at most once.

As illustrated in [@miller1960integer], a solution from IP model with constraint ([\[SG1\]](#SG1){reference-type="ref" reference="SG1"}), ([\[SG2\]](#SG2){reference-type="ref" reference="SG2"}) and ([\[continue\]](#continue){reference-type="ref" reference="continue"}) could contain *subtours*, which are cycles formed by edges that are disjoint from $\bar{P}_r$. [@miller1960integer] denotes that subtours can be eliminated by creating integer decision variables $3\leq u_{v_i,r}\leq |\bar{V}|$ for each $v_i\in \bar{V}\backslash \{\bar{X}^I(r),\bar{X}^G(r)\}$, thus, new constraint should be added into the proposed IP model: $$\begin{equation}
        u_{v_i,r}-u_{v_j,r}+|\bar{V}|x_{v_i,v_j,r}\leq |\bar{V}|-1;
\end{equation}$$

::: {#sol .proposition}
****Proposition** 1**. *Given $\langle \bar{G},\bar{X}^I,\bar{X}^G\rangle$, there exists a bijection between solutions to the topological-map-based IP model and all non-cyclic paths in $\bar{G}$ from $\bar{X}^I$ to $\bar{X}^G$.*
:::

::: Proof
**Proof 2**. **Injectivity.* Assume that adjacent matrix $x$ representing all the edge variables is a feasible solution of the topological-map-based IP model, a non-cyclic path starts from $\bar{X}^I(r)$ to $\bar{X}^G(r)$ can be achieved by following the positive edge variables in $x$ until reaching $\bar{X}^G(r)$.*

**Surjectivity.* Assume that $\bar{P}_r=\{p_r(t^0_r),\dots,p_r(t^0_r+T_r)\}$ is feasible, $\bar{P}_r$ can be converted into a feasible solution to the proposed IP model by assigning the corresponding edge variable in adjacent matrix $x$ to 1 and others to 0.  $\blacksquare$*
:::

After topological extraction, there could exist some $r$, $\bar{X}^I(r)=\bar{X}^G(r)$, we split $\bar{X}^I(r)(\bar{X}^G(r))$ into two vertices $v_{in},v_{out}\in V_c$ depending to the relative position of $X^I(r)$ and $X^G(r)$: $\bar{X}^I(r)=v_{in},\bar{X}^G(r)=v_{out}$.

**(One-way constraints)** As we denote in Lemma [1](#lemma1){reference-type="ref" reference="lemma1"}, for each narrow-passage in $G$, one-way constraint should be imposed to prevent the robots from causing collisions within the passages.

1.  if neither $\bar{X}^I(r)$ nor $\bar{X}^G(r)$ is in $PA_{v_p}$, the direction of robot $r$ in $PA_{v_p}$ can be represented as: $$\begin{equation}
            \label{dir1}
                d(v_p,r)=x_{v_i,v_p,r}+3x_{v_p,v_i,r};
    \end{equation}$$

2.  if $\bar{X}^I(r)$ is in $PA_{v_p}$, the direction of robot $r$ in $PA_{v_p}$ can be represented as: $$\begin{equation}
            \label{dir2}
                d(v_p,r)=x_{v_p,v_j,r}+3x_{v_p,v_i,r};
    \end{equation}$$

3.  if $\bar{X}^G(r)$ is in $PA_{v_p}$, the direction of robot $r$ in $PA_{v_p}$ can be represented as: $$\begin{equation}
            \label{dir3}
                d(v_p,r)=x_{v_i,v_p,r}+3x_{v_j,v_p,r};
    \end{equation}$$

::: proposition
****Proposition** 2**. *Equation [\[dir1\]](#dir1){reference-type="ref" reference="dir1"} to [\[dir3\]](#dir3){reference-type="ref" reference="dir3"} are able to denote all the directions of motion of all robots.*
:::

::: Proof
**Proof 3**. *With constraint [\[SG1\]](#SG1){reference-type="ref" reference="SG1"} to [\[continue\]](#continue){reference-type="ref" reference="continue"}, $P_r$ is continue and non-cyclic which is composed of vertices that do not repeat. Equation [\[dir1\]](#dir1){reference-type="ref" reference="dir1"} ensures that robot $r$ which starts from $v_j(v_i)$, then passes passage $PA_{v_p}$ and terminates at $v_i(v_j)$, whose direction in $PA_{v_p}$ can be denoted with $x_{v_i,v_p,r}=x_{v_p,v_j,r},x_{v_j,v_p,r}=x_{v_p,v_i,r}$. Equation [\[dir2\]](#dir2){reference-type="ref" reference="dir2"} and [\[dir3\]](#dir3){reference-type="ref" reference="dir3"} ensure that direction of robot $r$ can be denoted whose initial (goal) configuration is in $PA_{v_p}$.  $\blacksquare$*
:::

In general, the robot's direction in one passage can be $3$, $1$ and $0$ (the robot does not pass this passage). Direction $3$ and Direction $1$ are the opposed. Thus, the general direction of passage $PA_{v_p}$ can be represented as: $$\begin{equation}
 \label{dir}
     D(v_p)=\max_{r\in R}d(v_j,r).
\end{equation}$$ With equation [\[dir\]](#dir){reference-type="ref" reference="dir"}, similar to robot's direction, the direction of passage can be $3$, $1$ and $0$ (no robot passes this passage). Direction $3$ and Direction $1$ are the opposed. Thus, one-way constraint can be translated into propose IP model, for all the passages $PA_{v_p}$: $$\begin{equation}
 \label{OWcons}
     \forall r\in R,\quad D(v_p)-d(v_p,r)\neq 2.
\end{equation}$$

Now, all the constraints in topological-map-based IP model in $\bar{G}$ have been formulated with constraint [\[SG1\]](#SG1){reference-type="ref" reference="SG1"} to [\[OWcons\]](#OWcons){reference-type="ref" reference="OWcons"}.

**(Objective functions)** In this work, we work on two common objectives focusing on distance optimality: *Total Distance* and *Maximum Distance*. Now we translate them into IP model objective functions.

In topological map $\bar{G}$, for each edge $(v_i,v_p)\in \bar{E}$, we assume that $v_p$ is the passage vertex. Thus edge $(v_i,v_p)$ can be related to certain weight which is decided by the length of passage $|PA_{v_p}|$ (see Fig. [2](#project){reference-type="ref" reference="project"}). Denote the correlated weights as: $$\begin{equation}
     \{w_{v_i,v_p}=w_{v_p,v_i}=|v_p-v_i|\; | (v_i,v_p),(v_p,v_i)\in \bar{E}\}.
\end{equation}$$

**Minimize Total Distance:** For all $r\in R$, with binary decision variable $x_{v_i,v_j,r}$ for each edge $(v_i,v_j)\in \bar{E}$ to indicate whether $P_r$ uses $(v_i,v_j)$, the objective function of *total distance* can be denoted as: $$\begin{equation}
     \sum_{r\in R}\sum_{(v_i,v_j)\in \bar{E}}w_{v_i,v_j}x_{v_i,v_j,r};
\end{equation}$$

**Minimize Max Distance:** To denote the minimize max distance objective function, we introduce an additional integer decision variable $xmax$ and for all $r\in R$ add the constraint: $$\begin{equation}
 \label{minmax}
     \sum_{(v_i,v_j)\in \bar{E}}w_{v_i,v_j}x_{v_i,v_j,r}\leq xmax(r).
\end{equation}$$

In constraint [\[minmax\]](#minmax){reference-type="ref" reference="minmax"}, the left side represents the distance traveled by robot $r$. Thus, the objective function can be denoted as: $$\begin{equation}
     \min_{r\in R} xmax(r).
\end{equation}$$

As the collision avoidance in narrow passages is ensured by one-way constraint, the improvement of time optimality can be then considered by reducing the likelihood that the robot will wait at the intersection vertex. Here, new data structure $\mathcal{P}$ of dimension $n\times |V_c|$ is proposed to record the vertices $v\in V_c$ which are passed by robot $r$.

$$\begin{equation}
 \label{number}
     \mathcal{P}(r,v)=\max_{v_i\in V_c}{x_{v,v_i,r}}.
\end{equation}$$

Then we can denote the number of robots passing at the intersection vertex $v\in V_c$:

$$\begin{equation}
 \label{cost}
     C(v)=\sum_{r\in R}\mathcal{P}(r,v).
\end{equation}$$ In order to reduce the congestion in each intersection vertex, we can add $\max_{v\in V_c }{C(v)}$ to each distance optimality objective function to improve the time optimality. Now, with complete constraints and two objective functions, topological-map-based IP model has been formulated completely.

Here, suppose that scheduled path set $\bar{P}$ is the solution of the proposed IP model with proposition [1](#sol){reference-type="ref" reference="sol"}. We note that for all $v_i\in \bar{P}_r$, $v_i\in \bar{V}$, in order to retain the scheduled path set $P$ in $G$, vertices in $V$ should be added between $v_i$ and $v_j$, $v_i,v_j\in \bar{P}_r, (v_i,v_j)\in\bar{E}$. As the proposed IP model ensures that the passages being passed by robots are one-way, the initial and goal configuration $X^I$ and $X^G$ can be added into the scheduled path set directly. For path set $P$, we ensure that for all $t_r^0\leq t < t_r^0+T_r$: $$\begin{equation}
    |p_r(t+1)-p_r(t)|=1, p_r(t),p_r(t+1)\in V.
\end{equation}$$

The complete algorithm process can be seen in Fig. [2](#project){reference-type="ref" reference="project"} visually. With the three-step process of **OWIP**, we have the proposition :

::: proposition
****Proposition** 3**. *Algorithm **OWIP** is a complete algorithm for finding MPP solution in narrow-passage warehouse-like environment that minimize the total distance or minimize the max distance traveled by robots.*
:::

# Heuristic feasible initial solution

Intuitively, the time to solve an IP model is often influenced by the number of decision variables in the model, by topological extraction, the number of decision variables have been reduced significantly. In order to further increase the efficiency of solving the IP model, we consider that in cases where the IP solver is slow in finding an initial feasible solution, it can be helpful to provide a feasible solution along with the model itself. Thus, we propose a heuristic algorithm based on $\bar{G}$ to find the anytime-feasible initial solution in polynomial running time that satisfy the path continuity constraints and one-way constraints.

![\[Left\] Topological map with collision-ignorance path set of robots. \[Middle\] Topological map with one-way directions (indicated by black arrows). \[Right\] Topological map with initial feasible solutions.](initial.pdf){#heu width="\\linewidth"}

## Overview

The proposed heuristic algorithm adopts a four-step process. The key idea is that we base on the IP model input $\bar{X}^I,\bar{X}^G, \bar{G}$ to retain initial any-time feasible solution $\bar{P}^0$, then provide $\bar{P}^0$ along with the model itself to Gurobi solver.

::: algorithm
**Projection**: $x^I, x^G \leftarrow \mathrm{Projection}\left(\bar{X}^I,\bar{X}^G,\bar{G}\right)$

**Initial path planning**: $\mathcal{O},\mathcal{N} \leftarrow \mathrm{InitialPathPlan}(x^I,x^G,\bar{G})$

**One-way regulation**: $\bar{G}\leftarrow \mathrm{OneWayRegulation}(\mathcal{O},\mathcal{N},\bar{G})$

**Final path planning**: $\bar{P}^0\leftarrow \mathrm{FinalPathPlan}\left(\bar{X}^I,\bar{X}^G,\bar{G}\right)$
:::

## Projection

A projection from $\bar{X}^I$ and $\bar{X}^G$ in $V_p$ to $V_c$ is implemented in Algorithm [\[projectionPipe\]](#projectionPipe){reference-type="ref" reference="projectionPipe"}. It is possible that the two crossing vertices in both ends of narrow passage have the same Manhattan distance from $\bar{X}^I(r)$ or $\bar{X}^G(r)$. In such case, we can choose an arbitrary one from them.

::: algorithm
() $r \in R$

$x^I(r) = \mathop{\arg\min}_{v\in V_c} |\bar{X}^I(r) - v|$

$x^G(r) = \mathop{\arg\min}_{v\in V_c} |\bar{X}^G(r) - v|$
:::

## Initial path planning

Here, collision-ignore path set $\bar{P}^0$ is found with A\* search while ignoring other robots on $\bar{G}$. We note that the edge set associated with path $P_r$ on $\bar{G}$ can be defined as: $\mathcal{E}(P_r):=\left\{(P_r(t),P_r(t+1))|\forall 0\leq t < len(P_r), \left(P_r(t),P_r(t+1)\right) \in \bar{E}\right\}$.

We use an integer array $\mathcal{O}:|\bar{E}|\times 1$ which stores the sum of the distance spent by all the robots on each edge of $\bar{E}$. $\mathcal{N}:|\bar{E}|\times |R|$ is a binary matrix which indicates the edges traveled by each robot.

::: algorithm
$\mathcal{O}(\bar{E})=\mathbf{0}$

() $r\in R$

$\bar{P}^0_r=A^*\left(\bar{G},x^I(r),x^G(r)\right)$

() $e\in \mathcal{E}({\bar{P}^0_r})$

$\mathcal{O}(e)=\mathcal{O}(e)+|e|$

$\mathcal{N}(e,r)=1$
:::

## One-way regulation

In the previous step, the initial path for each robot is achieved ignoring other robots. Now, we introduce an mechanism *One-way Regulation* based on the one-way constraint to resolve collision. First, we define a topological gadget *0-instance* whose schematic diagram is given in the middle part of Fig. [3](#heu){reference-type="ref" reference="heu"}. In our proposed narrow-passage warehouse-like environment, each shelf can be surrounded by four crossing vertices: $v_i,v_j,v_m,v_n \in V_c$, and four passage vertices: $v'_i,v'_j,v'_m,v'_n$.

::: definition
****Definition** 3**. *A *0-instance* on $g$ is defined as a vertex loop on $g$ surrounding each shelf $s$, $s\in \{1,\dots,S\}$. We define the following *clockwise 0-instance* as a vertex loop set of adjacent vertices: $I_s:=\{v_i,v'_i,v_j,v'_j,v_m,v'_m,v_n,v'_n,v_i\}$ with *clockwise 0-instance* $I'_s:=\{v_i,v'_n,v_n,v'_m,v_m,v'_j,v_j,v'_i,v_i\}$.*
:::

Each vertex set $V_s$ has a corresponding edge set: $\mathcal{E}(V_s):=\{\left(V_s(i),V_s(i+1)\right)|\forall 1\leq i<|I_s|\}$ We introduce an *Instance Rotation* operation to switch a clockwise (counterclockwise) 0-instance to (counterclockwise) clockwise 0-instance. Now $\bar{G}$ is undirected, so that for any $s$, $I_s$ and $I'_s$ simultaneously exist, i.e., $\mathcal{E}(I_s)\in \bar{E}, \mathcal{E}(I'_s)\in \bar{E}$.

Because of one-way constraint, for any $s$, one and only one of $I_s$ and $I'_s$ can exist. In order to choose the better one between $I_s$ and $I'_s$, heuristic value for $I_s$ and $I'_s$ should be calculated.

Here, we propose a heuristic value $H_v(s)$ which represents the total detour cost: in order to satisfy the one-way constraint, some robots cannot follow the collision-ignore paths to move (see Fig [2](#project){reference-type="ref" reference="project"} for example). Thus, detour costs in scheduled paths are caused with respect to collision-ignore paths.

Total detour cost $D_t$ for $I_s$ and heuristic value $H_v(s)$ are proposed as:

$$\begin{equation}
\label{detour}
\begin{aligned}
        D_t(I_s)=\left(\sum\nolimits_{r\in \{1,2\dots,R\}}
        \min\left(
            \sum\nolimits_{e\in \mathcal{E}({I_s})}\mathcal{N}(e,r),1
            \right)
    \right)\\
    \cdot\sum\nolimits_{e\in \mathcal{E}(I_s)}|e|-\sum\nolimits_{e\in \mathcal{E}({I_s})}{\mathcal{O}(e).}
\end{aligned}
\end{equation}$$

::: algorithm
()$s\in\{1,\dots,S\}$

$H_v(s)=\max{(D_t(I_s),D_t(I'_s)}$

$I_s=\arg\max_{I\in\{I_s,I'_s\}}{(D_t(I))}$

Sort all $I_s, s\in\{1,\dots,S\}$ in the descending order of $H_v(s)$.

$E_n=\mathcal{E}(I_1),V_n=I_1$

()$s\in \{2,\dots,S\}$

$E_n=E_n+\mathcal{E}(I_s)-\mathcal{E}(V_n\cap I_s)$

$V_n=V_n+I_s$

$\bar{V}=V_n,\bar{E}=E_n$
:::

The *One-way Regulation* process is denoted in Algorithm. [\[DC\]](#DC){reference-type="ref" reference="DC"}. After one-way regulation, we know that $\bar{G}$ is directed: if $(v_i,v_j)\in \bar{E}$, then $(v_j,v_i)\notin \bar{E}$ and vice versa.

::: {#FeasibleMotion .theorem}
****Theorem** 1**. *Under one-way constraint, $\bar{G}$ satisfies that any $v_s,v_t\in \bar{V}$, there exists at least one path from $v_s$ to $v_t$ if each $\mathcal{E}({I_s})$ is regulated by *Instance Rotation*.*
:::

::: Proof
**Proof 4**. *It is no doubt that for all $s\in \{1,2,\dots,S\}$, $v_1,v_2\in I_s$, there exists a path from $v_1$ to $v_2$ as all the edges in $\mathcal{E}({I_s})$ are connected end to end. Furthermore, $\forall e\in\bar{E},\exists s\in \{1,2,\dots,S\},e\in \mathcal{E}({I_s})$ can be confirmed with Algorithm [\[DC\]](#DC){reference-type="ref" reference="DC"}. Thus, theorem [1](#FeasibleMotion){reference-type="ref" reference="FeasibleMotion"} can be satisfied.$\blacksquare$*
:::

:::: {#cmp .figure latex-placement="ht"}
![](Huo2021IntegerProgrammingBased_figs/initialcompare.png){width="100%"}

::: caption
\[Left\]Running time comparison for heuristic feasible initial solution. \[Right\]Simulation environment.
:::
::::

The left part of Figure. [4](#cmp){reference-type="ref" reference="cmp"} proves the effectiveness of heuristic feasible initial solution to reduce the running time of OMRPP.

## Final path planning

Here, the one-way passages are formed on the directed $\bar{G}$. Feasible initial solution $\bar{P}^0$ is found by applying heuristic $A^*$ algorithm. With proposition [1](#sol){reference-type="ref" reference="sol"}, initial solutions value for decision variables $x$ can be assigned by $\bar{P}^0$.

::: proposition
****Proposition** 4**. *Time complexity of heuristic feasible initial solution algorithm is polynomial ($O(n|\bar{V}|^2)$).*
:::

::: Proof
**Proof 5**. *In Algorithm [\[overview\]](#overview){reference-type="ref" reference="overview"}, projection (Line 1) has time complexity of $O\left(n \right)$. In Line 2, we apply the heuristic $A^*$ search which has time complexity [^2] of $O(n|\bar{V}|^2)$ . In Line 3, *One-way regulation* can be terminated in $O(n|\bar{V}|^2+|\bar{V}|^2)$. In Line 4 the final paths can be searched in $O(n|\bar{V}|^2)$ with heuristic $A^*$ Algorithm. Thus, **OMRPP** has time complexity of $O(n|\bar{V}|^2)$. $\blacksquare$*
:::

# Experiments and results

:::: {#res .figure latex-placement="ht"}
![](Huo2021IntegerProgrammingBased_figs/resPDF.png){width="100%"}

::: caption
\[Upper\]Simulation results of total time and total distance optimality. \[Lower\]Simulation results of maximum time and maximum distance optimality.
:::
::::

In this work, we evaluate **OMRPP** on warehouse-like narrow-passage environment. Here, we represent part of results in this section. We construct a $21\times 22$ such grid environment (right part of Figure [4](#cmp){reference-type="ref" reference="cmp"}) for simulation.

## Experiment setup

We implement **OMRPP** and CBS in MATLAB. Concerning k-way split IP, we use the existing implementation by [@yu2016optimal] written in Java. We use PIBT method by [@okumura2019priority] written in C++. Here we note that the constructed IP models in **OMRPP** are solved using Gurobi Solver (Academic Version 9.1.1). All experiments are executed on an Intel(R) Xeon(R) CPU E5-2650 v4 with 128GB RAM at 2.20GHz on Ubuntu 5.4.0. The experimental evaluation has been done on diverse instances consisting of robot set with random initial and goal configuration ranging in the number of robots from small to large. We also compare the running time of IP-based MPP algorithms, i.e., **OMRPP**, IP exact and IP k-way split $k=2,4$.

We vary the number of robots from 10 to 50 while initial and goal configurations of robots are generated randomly for each single robot in robot set.

## TDMPP and TTMPP problem

Simulation results are represented in the upper part of Fig. [5](#res){reference-type="ref" reference="res"}. We present *optimality ratio* which is measured as the solution cost over an underestimated cost, i.e., the solution cost of the paths set ignoring the robot collision.

The general tend we can observe from Fig. [5](#res){reference-type="ref" reference="res"} indicates that the performance of **OMRPP** degrades as number of robots gets bigger. Comparing with PIBT and IP 4-way split, **OMRPP** has better performance while CBS, IP exact and IP 2-way split achieve lower solution cost but longer running time (CBS cannot be solved in $10^4$ seconds with robots exceeding 20 robot). For each number of robot, 10 times of test are done to achieve the mean value for optimality ratio and *running time* for each number of robot, i.e., time to achieve the optimal solution of IP model.

Thus, we can conclude that in case of TDMPP and TTMPP problem, **OMRPP** has a relatively efficient performance when the number of robots increases.

## MDMPP and MTMPP problem

In case of MDMPP and MTMPP problem, we can see the performance of **OMRPP** under the narrow-passage warehouse-like environment in the lower part of Fig. [5](#res){reference-type="ref" reference="res"}. In comparison with PIBT, **OMRPP** always holds better optimality performance. Taking the other IP-based methods into consideration, when the number of robots grows, the optimality performance of **OMRPP** augments and exceeds IP 4-way split. Meanwhile, IP k-way split solves more quickly the problem with small number of robots than **OMRPP**, but the growth of **OMRPP** running time is much lower than IP 2-way split and IP exact, which can prove the efficiency of **OMRPP** under the narrow-passage warehouse-like environment.

# Conclusion

In this work, we provide a fast and high-efficient algorithm to tackle the MPP problem in narrow-passage warehouse-like environments, which guarantees to generate collision-free paths. We tackle the bottleneck of MPP algorithms in narrow-passage environments with the proposal of anytime feasibility property based on one-way constraint. The proposed algorithm combines advantages of efficient running time and optimality performance. In future work, we would like to apply **OMRPP** heuristics for solving MPP problems beyond warehouse-like environment.

[^1]: All the authors are with the College of Electrical Engineering, Zhejiang University, Hangzhou, 310027, China (Email: jiaxi.huo@zju.edu.cn, rzheng@zju.edu.cn, liumeiqin@zju.edu.cn, slzhang@zju.edu.cn)

[^2]: *We reduce the time complexity of $A^*$ search to the time complexity of Dijkstra search by improving the operations on $OPEN$ list. Explanation is omitted here due to space limitation.*
