# Module2 Planner Calibration Sweep Protocol

Status line: diagnostic, non-formal, no paper claims.

## Scope Lock

Approved by Dr Sun on 2026-07-08 (Cursor session, option "a_and_b": housekeeping
+ calibration sweep). Purpose: test the **stability** of the planner-probe
signal (2026-07-08, `0_trials/module2_planner_probe/`) on fresh query seeds at
larger scale, and measure per-set variance so that the lane-C formal contract
thresholds can be calibrated from data instead of from a single 40-query
bucket. Rationale precedent: encoder-pilot R3's 0.8125 was small-eval luck
that led directly into the Gate3 v3 formal FAIL; thresholds must not be locked
off one small sample again.

This sweep produces NO band verdict and NO formal evidence. Its only output is
a variance/stability table that informs the lane-C contract draft. The formal
run (step D) will use yet other fresh query seeds.

No training is permitted. Inference/planning only.

## Methods

Same three methods as the probe, same harness (`forest_n3p.main_evaluation`),
all three run in a single `run_main_evaluation` invocation per query-seed set:

- M1 `ha_single_rs` — vanilla HA*, analytic operator single_rs.
- M2 `ha_dang_multi_rs` — Dang multi-curvature RS.
- M3 `ha_rl_rs_ppo` — HA* + RL-RS funnel operator, deterministic policy,
  frozen Gate3-failed checkpoint (below).

## Frozen Run Parameters

- Checkpoint: `0_trials/module2_gate3_formal_v3/seed20260709/train/final_model.zip`
- Checkpoint SHA256: `ae97501bd75f4d832bd400c9a6b42e8f73b71ab6a955fa1cb956a155357fc1a2`
  (git-ignored binary; verify by SHA256 against
  `seed20260709/gate3_trial_manifest.json` before running)
- Query-set base seeds: `20260810`, `20260910`, `20261010` (three independent sets)
- Queries per bucket: `100`; seed_count: `5`; queries_per_map: `5`
- Density profiles: `validation_t06`
- Distance bins: default `8:12,12:16,16:20,20:`
- Buckets generated by the harness: Easy + Complex + Extreme. **Calibration
  analysis is pre-registered on Complex and Extreme only**; Easy rows are
  recorded and reported as a sanity check (RL operator must not degrade Easy),
  they enter no threshold calibration.
- M3 checkpoint inference device: `cuda`
- RL-RS observation/rollout parameters: existing defaults, identical to the
  probe (patch 6.4 m / 64 cells, EDT clip 2.0 m, max steps 32, action step
  0.3 m, collision sample step 0.1 m, terminal check every 1, no-progress
  patience 3)
- Timing/timeout/node protocol: existing `MainEvaluationConfig` defaults
  (teacher timeout 2.5 s, teacher max nodes 15000); timing includes NN
  forward, rollout, collision checks — no hidden costs
- Bootstrap resamples: `5000`
- Execution host: gpu3070ti-relay (same host class as probe timing evidence),
  training venv interpreter
  `/home/ubuntu/ForestNav.pre_git_20260707T101154Z/.venv/bin/python`,
  `env -u FORESTNAV_SOURCE_HEAD PYTHONPATH=2_experiment KMP_DUPLICATE_LIB_OK=TRUE`
- Command per set (only `--seed` and `--output-dir` vary):

```text
python -m forest_n3p.scripts.run_main_evaluation \
  --output-dir 0_trials/module2_planner_calibration/set_s<BASE_SEED> \
  --seed <BASE_SEED> \
  --queries-per-bucket 100 --seed-count 5 --queries-per-map 5 \
  --methods ha_single_rs,ha_dang_multi_rs,ha_rl_rs_ppo \
  --density-profile-buckets validation_t06 \
  --module2-rl-rs-checkpoint 0_trials/module2_gate3_formal_v3/seed20260709/train/final_model.zip \
  --module2-rl-rs-device cuda \
  --allow-unresolved-human-review
```

## Query-Seed Disjointness (verified before commit, local Mac)

The three base seeds were checked with `build_query_set` against the probe
manifest and each other: no shared `map_seed` and no shared `query_seed` in
any bucket.

```text
base_seed=20260810 bucket_counts={'Easy': 100, 'Complex': 100, 'Extreme': 100} unique_maps=60
base_seed=20260910 bucket_counts={'Easy': 100, 'Complex': 100, 'Extreme': 100} unique_maps=60
base_seed=20261010 bucket_counts={'Easy': 100, 'Complex': 100, 'Extreme': 100} unique_maps=60
{
  "probe_map_seed_count": 20,
  "disjoint_from_probe_and_each_other": true,
  "problems": []
}
```

Queries come from the same held-out `build_query_set` mechanism as the probe
and are disjoint from all training/curriculum data by construction (different
generator and seed space).

## Pre-Registered Analysis (computed after all three sets finish)

Per set x bucket (Complex, Extreme) x method: success rate, timeout rate,
median/p95 expansions, median/p95 end-to-end time, collision violations
(must be 0), M3 operator telemetry (rl_attempts, rl_successes, mean
nn_forward_time_s, fallbacks). Paired per-query M3 vs M1 median expansions
ratio and median time ratio. Path-length comparison M3 vs M1 on jointly
successful queries (the standard runner has no vanilla reference method, so
inflation is computed in the aggregation step from paired path lengths).

Across the three sets, for each headline quantity report min / median / max —
that spread is the calibration deliverable. Explicitly reported quantities:

1. Complex success delta (M3 - M1), pp.
2. Complex paired median time ratio (M3/M1).
3. Complex paired median expansions ratio (M3/M1).
4. Extreme success delta (M3 - M1), pp.
5. Extreme paired median time ratio (M3/M1).
6. Easy success delta (M3 - M1), pp (sanity check only).

No thresholds, no bands, no verdict here: threshold-setting happens in the
lane-C contract draft, which Dr Sun reviews and approves separately.

## Frozen-Protocol Rules

- Protocol frozen at commit time: no query-set change, no timeout change, no
  operator-parameter tuning after any result exists.
- An M3 crash on a query counts as an M3 failure; fix nothing. A crash that
  also breaks M1/M2 is an infrastructure bug: stop and report to Dr Sun.
- Remote git HEAD must equal local HEAD before each run; worktree clean.
- Budget: if projected total wall clock exceeds 6 h after the first set,
  stop and report instead of shrinking the protocol. Two consecutive
  gpu3070ti-relay connection failures = stop.
- No force-push, no history rewriting, no `FORESTNAV_SOURCE_HEAD`.

## Progress

### 2026-07-08 remote sync and environment verification

- Remote worktree initially refused the pull: the probe M3 files were locally
  modified relative to the old remote HEAD `0de18b9b` (they held the rerun
  content rsynced during the probe, identical to `origin/main` by SHA256 —
  verified per file before touching anything). `git checkout -- M3/` then
  `git pull --ff-only` fast-forwarded cleanly.
- Remote HEAD after pull: `6b27edeb39b290e0a8045ab8e2895ae89a828f9d` (equals
  local HEAD at protocol commit); `git status --short` empty.
- Checkpoint SHA256 on remote: `ae97501b...357fc1a2` — matches the manifest.
- Training venv import smoke: `env_ok cuda= True`; GPU idle (31 MiB used).

### 2026-07-08 set 1 (base seed 20260810)

- Launched 10:40 remote time via nohup; completed in ~20 minutes:
  900 records (300 queries x 3 methods), `status: candidate_or_smoke`,
  `formal_acceptance: false` (expected: not the official T14 method list),
  `collision_violation_total: 0`, `method_exception_total: 0`.
- Budget check (first-set projection): 3 sets x ~20 min ≈ 1 h total, far
  below the 6 h cap — continue.
- Artifacts rsynced to the local Mac; success rates independently recomputed
  from `records.csv` and match `summary_by_method_bucket.csv` exactly
  (M1 0.910/0.850/0.670, M2 0.890/0.870/0.680, M3 0.990/0.920/0.810 for
  Easy/Complex/Extreme).
- Known cosmetic defect, recorded not fixed (frozen protocol): remote
  `run_config.json` has `source_head: unknown` because the harness's git
  lookup does not see the relay's non-PATH git install. Provenance is covered
  by the ssh-verified HEAD match above; the same applies to sets 2-3.

### 2026-07-08 sets 2-3 driver

- Sets 20260910 and 20261010 launched sequentially via a nohup'd driver loop
  on the relay; per-set exit codes append to `sets23_driver.log`.
