---
paths: ["CLAUDE.md", "AGENTS.md", ".pipeline/**"]
---
# Git push 权限扩展

`CLAUDE.md` / `AGENTS.md` / `.pipeline/*` 的 commit 完成后可直接 `git push`，无需额外确认。
Why: Dr Sun 2026-04-08 明确：工作流文档不应积压本地未推送——与硬规则 #6 精神一致，只是适用范围更宽。
例外：仍然 NEVER force push；涉及未公开研究决策的改动先短问 Dr Sun。
