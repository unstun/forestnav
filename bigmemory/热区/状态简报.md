# 项目状态简报
> 最后更新：2026-07-05

## 当前在做什么
- 模块2 RL 替换 RS 已进入 formal-gate 收口阶段；当前顶层 gate 是 `protocol_lane_decision`，不是旧的 F02.6 shorthand。
- 当前唯一允许的下一步是 `record_protocol_lane_decision`，且必须由 Dr Sun 选择路线并给出可审计 rationale。
- 当前仍禁止：本地 PPO 训练、远端新 success training、远端新 preflight、H01/H02 formal evaluation、formal claim、论文结果/appendix 结果材料。

## 关键上下文
- 模块2定义为“HA* analytic expansion 槽内的 RL-RS operator”，不是独立 RL planner，也不是 F-N3P subgoal 外挂。
- 已有 warm-start Gate3 formal run 失败：terminal-RS success `0.53125 < 0.8`；该失败只能作为负证据或下一轮 protocol 设计依据，不能作为 PPO 替代 RS 成功。
- 当前四条 protocol lane：`stronger_obstacle_summary_warm_start`、`full_patch_cnn_policy`、`hybrid_ppo_analytic_fallback`、`stop_or_reframe_module2_claim`。
- 若继续追求成功，需要先记录 protocol lane，再产出新的或修订后的 Research Contract，且状态必须是 `approved` 或 `frozen`；draft contract 不授权训练。
- 下一轮仍缺 formal 产物：远端 checkpoint bundle、`train/summary.json`、`training_manifest.json`、formal Gate3 eval CSV/summary、Gate3 audit pass、checkpoint hash、H02 `formal_output_accepted=true`。

## 近期警告
- 旧 F02.6/warm-start readiness、旧 remote packet ready、远端 smoke 或 H02 smoke 都不是当前新 success attempt 授权。
- 当前 `protocol_lane_decision_record` 仍是 `pending_protocol_lane_decision`；`selected_lane_id=None`。
- `protocol_lane_decision` 只会打开 contract drafting gate，不会直接打开 remote preflight/training。
- `gpu3070ti-relay` 可作为后续正式远端资源，但当前不能启动新 preflight/training；本地训练仍禁止。
