# 项目状态简报
> 最后更新：2026-07-08

## 当前在做什么
- 模块2（HA* analytic expansion 槽内的 RL-RS operator）：Gate3 formal v3 已判 **FAIL**（gate seed 20260706 成功率 0.6836 < 0.80；最好的陪跑 seed 20260709 = 0.7109）。
- 但 2026-07-08 完成的 planner-integration 诊断探针（非正式）给出**正信号**：把失败的 seed-20260709 checkpoint 装进真实 Hybrid A* 后，Complex 桶成功率 0.900 vs vanilla 0.675（+22.5pp），中位扩展数 1 vs 737（约 -99%），端到端约快 15%；Extreme 桶 +5pp、扩展数 -96%、时间持平；全方法 0 碰撞。产物：`0_trials/module2_planner_probe/`。
- Dr Sun 已拍板路线（2026-07-08，Cursor 会话）：**A 家务事 → B 校准复测（非正式，新查询种子 2–3 组 × 每桶 100 题，同一冻结 checkpoint）→ C 起草 lane-C 正式契约（RL 辅助解析扩展 + 安全兜底，主指标押 Complex 桶）→ D 正式实验（Codex /goal，gpu3070ti-relay）**。当前进行到 B。
- 跨 agent 状态源：forestnav2 仓库 `docs/plans/module2-STATUS.md`（先读它）。

## 关键上下文
- 模块2定义为"HA* analytic expansion 槽内的 RL-RS operator"，不是独立 RL planner，也不是 F-N3P subgoal 外挂。
- 探针的机械 band 判定是 `no_signal`，这是 **band 定义缺陷**（只看 Extreme、未预设"扩展数大减且时间不变差"的形态），不是真负结果；放大时必须修 band。
- 探针中 RL 单次出手命中率不高（Complex 0.155 / Extreme 0.056），但命中即终结搜索、脱靶有 fallback——lane-C 叙事的核心机制。
- Gate3 v3 的 `obstacle_bypass` 评估阶段退化（三个 seed 全部 0.5179 成功 / 0.4821 碰撞，数字完全相同）；修复 + 再训练（SAC/HER/1M steps）已**推迟**为可选的后续独立契约，不在当前关键路径。
- 编码器试点 R3 的 0.8125 是小样本运气 → 直接导致 Gate3 formal 失败。这是 B 步存在的理由：阈值必须用 ≥100 题/桶的方差数据校准，不能用 40 题定契约。
- checkpoint 冻结：`0_trials/module2_gate3_formal_v3/seed20260709/train/final_model.zip`，SHA256 `ae97501b…357fc1a2`（git-ignored，以 manifest 的 SHA256 验证）。

## 近期警告
- 仍然禁止：本地训练、无契约的正式实验、任何 paper claim（探针与 B 步复测都是非正式诊断）。
- B 步跑之前必须先提交协议文件（协议先行，冻结后不许改查询集/超时/参数）。
- 正式实验（D 步）用全新查询种子，与探针 20260710 及 B 步种子互斥。
- 旧的 `protocol_lane_decision` 流程产物（0_trials/module2_formal_gate_* 一大批目录）是历史遗迹，不再是当前 gate 链；不要恢复那套 meta-audit 工作流。
- 不许 force-push；~837 个流水账提交的历史债保持"标记不动"。
