# 项目状态简报
> 最后更新：2026-07-02 02:05

## 当前在做什么
- 模块2（PPO 转向策略替代 RS 解析扩展）设计骨架已定案（`.pipeline/survey/module2-ppo-analytic-expansion-design.md`，commit 79a6b34b）：模块2先行/模块1挂起、定义A（PPO policy 直接替代 RS，从当前节点连最终目标）、训练用 PPO（RL）、输入=自适应局部地图（闭环 policy 每步局部 patch 观测+目标相对位姿→连续曲率）。
- 下一步做 Contract 前可执行的两个 kill-fast gate：gate#2（oracle 形态分析）、gate#1（成本账 microbenchmark）；gate#3（PPO 收敛试点）须 Contract approved 后才能做。
- 仍处规划阶段（规划→实验+分析→写作），Research Contract 尚未起草，未写任何实验代码（硬规则 #20）。

## 关键上下文
- "改启发式"路线已证伪；RS 解析扩展成功率是 HA* 速度与路径质量的共同根源，森林窄通道 RS 大面积失败是 F-N3P 变慢变差的根因。
- 双模块新颖性查重：模块1（运动基元剪枝）先例 Franke2025（arXiv:2503.05539），深读后发现其预生成+缓存非 per-node 推理、30% 加速口径剔除了推理时间（不诚实指标）、障碍仅标量密度编码（弱条件化）——这正是模块2 与之的差异空间。模块2（学习型解析扩展）联网复核仍确认空白：最近邻 Sivaramakrishnan2021(arXiv:2110.04238)/RL-RRT(arXiv:1907.04799) 均作用于采样式规划器而非 HA*；核实 Kim2021Neural 的 CVAE 动的是 node expansion 非解析扩展，差异坐实。
- 代码事实：planner 在 `2_experiment/forest_n3p/`（Python），分支因子10；RS 解析扩展位于 `planner.py:204` 自适应 interval 触发，失败落回基元扩展——是模块2 的干净插槽；训练基础设施齐全。

## 近期警告
- ⚠ Research Contract 未起草前禁止写实验代码或作为实验依据（硬规则 #20）；gate#3(PPO收敛试点) 须 Contract approved 后才能做。
- ⚠ F-N3P 把 RS 验证段耗时记为 0（`inference.py`），端到端诚实计时须先修，否则 gate#1 成本账会失真。
- ⚠ `key-papers/` 中的 MD 为独立拷贝，修改需同步更新 `papers/md/` 真源。
