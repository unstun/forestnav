# 项目状态简报
> 最后更新：2026-07-04 01:12

## 当前在做什么
- 模块2 RL 替换 RS 已进入论文级实现/验证阶段；Contract 已 approved，不再是“未起草”状态。
- 当前主阻塞是 F02.6 warm-start 人类决策 pending，以及缺正式 PPO checkpoint。
- H01/H02/I01-I03 已有协议、smoke、图表和 claim guard；A02.3 P0 telemetry 已接入 evaluation outputs，但 formal 性能 claim 仍 blocked。

## 关键上下文
- 模块2定义为“HA* analytic expansion 槽内的 RL-RS operator”，不是独立 RL planner，也不是 F-N3P subgoal 外挂。
- no-warm PPO Gate #3 formal 已失败：64 episode 中 terminal-RS success 29/64=0.453125，低于 0.8；只证明 no-warm 分支失败。
- F02.6 决策包推荐 obstacle-summary BC 作为 PPO practical warm-start，但只有 Dr Sun 可批准/驳回；当前 `remote_training_allowed=false`。
- H01 manifest 当前 blockers：`f02_6_warm_start_decision_pending`、`missing_module2_rl_rs_checkpoint`。
- `gpu3070ti-relay` 已验证 CUDA/SB3/Torch 可用；正式 PPO 训练/评测只能走该远端 GPU，禁止本地训练。

## 近期警告
- ⚠ 远端 warm-start CUDA smoke 是链路 smoke，audit 为 `not_formal`，不能当 formal checkpoint 或论文结果。
- ⚠ A02.3 P0 telemetry 已实现并通过 112 tests；后续正式表格仍必须等待 H02 formal data，不能用 smoke 填论文结果。
- ⚠ F02.6 未关闭前，不能启动 warm-start formal 训练，也不能把 no-warm 失败偷换成 warm-start 失败。
