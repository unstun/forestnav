## 03:54 — 文献库 MinerU VLM 公式修复通道入库
- **原因**：`1_survey/papers/` 中存在 `pymupdf4llm + formulas=0` 的公式风险 Markdown，继续保留会影响后续文献阅读、引用抽取和论文写作依据。
- **改动**：新增 `1_survey/scripts/batch_mineru_vlm.py`，通过 `/Users/sun/.local/bin/mineru-open-api extract --model vlm --formula=true --table=true` 批量转换；先写入 `1_survey/papers/api_trials/mineru_vlm_batch/<CitationKey>/`，质量门槛通过后替换主库 MD 和 `_figs`；`.gitignore` 增加 `1_survey/papers/api_trials/`；新增 `.claude/skills/mineru-pdf-to-md/SKILL.md`，并保留本地 Codex 可见副本 `.agents/skills/mineru-pdf-to-md/SKILL.md`（该副本被 `.gitignore` 忽略）。
- **训练/推理**：无训练；使用 MinerU Open API VLM 做 PDF 到 Markdown 转换，最终 96 篇入库，成功库中 `pymupdf4llm + formulas=0` 风险项降为 0。
- **状态**：已完成并提交，相关提交包括 `2a2cd8a1`、`894fdd35`、`e1a34f96`。未来需要知道：公式修复的可信路径已经从本地推理/marker 失败路径切换为 MinerU VLM API staged-replace 流程，后续不要再把旧 pymupdf4llm 结果当作公式完整版本。

## 03:54 — 文献库搜索、转换状态和验收记录刷新
- **原因**：本次修复不只是单篇转换，而是补齐文献库的系统性质量状态；后续调研需要知道当前语料规模、经典 seed 覆盖和转换成功率的基线。
- **改动**：更新 `paper_list.csv` 到 489 个候选；更新 `conversion_status.csv` 到 489 行状态（484 success、5 failed）；更新 `1_survey/papers/md/README.md` 和 `.pipeline/literature/index.md`；新增验收记录 `1_survey/papers/md_conversion_logs/2026-06-25_quality_repair_acceptance.md`。搜索/语料修复结果为标题相关性不达标 3/489=0.61%，经典 seed 15/15 入库，弱方向 A=13、B=33、M=13、P=15；目录非 README Markdown 为 523。
- **训练/推理**：无训练；转换统计来自批量 PDF/Markdown 处理和验收脚本结果。
- **状态**：已完成并提交，相关提交包括 `dd84c7b5`、`92b4cf66`。未来需要知道：`1_survey/papers/` 当前不是临时 scrape，而是经过搜索覆盖、转换状态和公式风险验收的一版可继续建设的文献语料基线。

## 08:38 — codex 产出 0_trials 路径对比可视化试跑
- **原因**：Dr Sun 让 codex 核验 goal，产物为 F-N3P vs vanilla 30 query 路径对比可视化，用于直观确认 F-N3P 路径质量差异的真实情况。
- **改动**：`0_trials/20260625_0620_bucket_trial/` 下新增路径对比图、`trial_records.csv`、`summary.md`；由 codex 提交，commit `2530b612`。本次 Claude 未改任何源码，仅做核验与调研（pure read-only）。
- **训练/推理**：无训练；为一次性可视化试跑，非正式受控实验，不挂 Research Contract。
- **状态**：已完成并提交（`2530b612`）。未来需要知道：该试跑结果中实测均值≈4.3%、中位≈0.1%、P95≈25%，与 deep-research 报告推算的"平均 8%"不符，不可将推算值当实测基线引用。
